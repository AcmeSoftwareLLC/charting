name: acme_chart_example
description: A Flutter application showcasing the features of the acme_chart package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ^3.44.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  acme_chart:
  provider: ^6.0.5
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
